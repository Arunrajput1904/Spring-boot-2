version: 0.2

phases:
  build:
    commands:
      - echo Build started...
      - mvn clean package -DskipTests

  post_build:
    commands:
      - echo Build completed successfully

artifacts:
  files:
    - target/*.jar
  discard-paths: yes

cache:
  paths:
    - /root/.m2/**/*