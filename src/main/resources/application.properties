spring.application.name=HiderJpa
server.port=8081

# ? Correct DB name (fix typo: hyberjpa ? hiberjpa)
spring.datasource.url=jdbc:postgresql://localhost:5433/hiberjpa
spring.datasource.username=postgres
spring.datasource.password=Arun@2003
spring.flyway.baseline-on-migrate=true
spring.flyway.enabled=false
spring.flyway.locations=classpath:db/migration

# VERY IMPORTANT

spring.sql.init.mode=never

# Hibernate should NOT create schema
spring.jpa.hibernate.ddl-auto=validate
